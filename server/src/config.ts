

export let config = {
    databases: {
        data: {
            host: "mongodb://localhost/resaroomdb"
        },
        user: {
            host: "mongodb://localhost/resaroomdb"
        }
    },
    server: {
        port: 3000
    }
};