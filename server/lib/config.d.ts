export declare let config: {
    databases: {
        data: {
            host: string;
        };
        user: {
            host: string;
        };
    };
    server: {
        port: number;
    };
};
